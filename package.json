{
  "name": "@jobsy-it/design",
  "version": "1.0.0",
  "private": false,
  "exports": {
    "./tokens.css": "./tokens.css",
    "./theme.css": "./theme.css",
    "./jobsy.css": "./dist/jobsy.css"
  },
  "files": [
    "tokens.css",
    "theme.css",
    "dist"
  ],
  "scripts": {
    "build": "tailwindcss -i main.css -o dist/jobsy.css --minify",
    "version": "npm run build && git add dist"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
